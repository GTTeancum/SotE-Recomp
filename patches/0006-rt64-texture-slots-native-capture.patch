diff --git a/src/hle/rt64_rdp_tmem.cpp b/src/hle/rt64_rdp_tmem.cpp
index 6051f4e..7d076ff 100644
--- a/src/hle/rt64_rdp_tmem.cpp
+++ b/src/hle/rt64_rdp_tmem.cpp
@@ -52,30 +52,48 @@ namespace RT64 {
     uint64_t TextureManager::uploadTexture(State *state, const LoadTile &loadTile, TextureCache *textureCache, uint64_t creationFrame, uint16_t width, uint16_t height, uint32_t tlut) {
         const uint8_t *TMEM = reinterpret_cast<const uint8_t *>(state->rdp->TMEM);
         uint64_t hash = TMEMHasher::hash(TMEM, loadTile, width, height, tlut, TMEMHasher::CurrentHashVersion);
+        const uint64_t contentHash = hash;
+        uint64_t sourceHash = 0;
+        if (sourceReplacement) {
+            sourceHash = sourceReplacement(replacementContext ? replacementContext() : 0,
+                state->rdp->rice.lastLoadOpByTMEM[loadTile.tmem], loadTile, width, height, tlut,
+                tlut ? state->rdp->rice.lastLoadOpByTMEM[256 + (loadTile.siz == 0 ? loadTile.palette * 16 : 0)].texture.address : 0);
+            if (sourceHash != 0) {
+                // Scope the cache entry as well as the replacement lookup.
+                // Keep changing texels in the key: missing replacements must
+                // continue animating, and identical images in different
+                // source slots must not share the wrong replacement.
+                const uint64_t key[] = { contentHash, sourceHash };
+                hash = XXH3_64bits(key, sizeof(key));
+            }
+        }
         if (hashSet.find(hash) == hashSet.end()) {
             hashSet.insert(hash);
-            textureCache->queueGPUUploadTMEM(hash, creationFrame, TMEM, RDP_TMEM_BYTES, width, height, tlut, loadTile, true);
+            textureCache->queueGPUUploadTMEM(hash, creationFrame, TMEM, RDP_TMEM_BYTES, width, height, tlut, loadTile, true, contentHash, sourceHash);
         }
 
         // Dump memory contents into a file if the process is active.
         if (!state->dumpingTexturesDirectory.empty()) {
-            dumpTexture(hash, state, loadTile, width, height, tlut);
+            dumpTexture(contentHash, state, loadTile, width, height, tlut);
         }
 
         return hash;
     }
 
     void TextureManager::dumpTexture(uint64_t hash, State *state, const LoadTile &loadTile, uint16_t width, uint16_t height, uint32_t tlut) {
-        if (dumpedSet.find(hash) != dumpedSet.end()) {
-            return;
-        }
-
-        // Insert into set regardless of whether the dump is successful or not.
-        dumpedSet.insert(hash);
+        const LoadOperation &sourceOp = state->rdp->rice.lastLoadOpByTMEM[loadTile.tmem];
+        const uint32_t context = replacementContext ? replacementContext() : 0;
+        const uint64_t dumpKey[] = { hash, context, sourceOp.texture.address,
+            sourceOp.texture.width, sourceOp.tile.uls, sourceOp.tile.ult,
+            sourceOp.texture.fmt, sourceOp.texture.siz, uint32_t(sourceOp.type),
+            width, height, loadTile.fmt, loadTile.siz, loadTile.line, loadTile.tmem,
+            loadTile.palette, tlut, tlut ? state->rdp->rice.lastLoadOpByTMEM[256 + (loadTile.siz == 0 ? loadTile.palette * 16 : 0)].texture.address : 0 };
+        const uint64_t dumpIdentity = XXH3_64bits(dumpKey, sizeof(dumpKey));
+        if (!dumpedSet.insert(dumpIdentity).second) return;
         
         // Dump the entirety of TMEM.
         char baseName[64];
-        snprintf(baseName, sizeof(baseName), "%016" PRIx64 ".v%u", hash, TMEMHasher::CurrentHashVersion);
+        snprintf(baseName, sizeof(baseName), "%016" PRIx64 ".v%u.s%016" PRIx64, hash, TMEMHasher::CurrentHashVersion, dumpIdentity);
         std::filesystem::path dumpTmemPath = state->dumpingTexturesDirectory / (std::string(baseName) + ".tmem");
         std::ofstream dumpTmemStream(dumpTmemPath, std::ios::binary);
         if (dumpTmemStream.is_open()) {
@@ -172,6 +190,8 @@ namespace RT64 {
             jroot["tile"] = loadTile;
             jroot["width"] = width;
             jroot["height"] = height;
+            jroot["paletteAddress"] = tlut ? state->rdp->rice.lastLoadOpByTMEM[256 + (loadTile.siz == 0 ? loadTile.palette * 16 : 0)].texture.address : 0;
+            if (replacementContext) jroot["sourceContext"] = replacementContext();
 
             // Serialize the TLUT into an enum instead.
             if (tlut == G_TT_RGBA16) {
@@ -194,4 +214,4 @@ namespace RT64 {
             hashSet.erase(hash);
         }
     }
-};
\ No newline at end of file
+};
diff --git a/src/hle/rt64_rdp_tmem.h b/src/hle/rt64_rdp_tmem.h
index 3708220..38e0aa6 100644
--- a/src/hle/rt64_rdp_tmem.h
+++ b/src/hle/rt64_rdp_tmem.h
@@ -5,6 +5,7 @@
 #pragma once
 
 #include <set>
+#include <functional>
 
 #include "hle/rt64_draw_call.h"
 #include "render/rt64_texture_cache.h"
@@ -15,6 +16,9 @@ namespace RT64 {
     struct TextureManager {
         std::set<uint64_t> hashSet;
         std::set<uint64_t> dumpedSet;
+        // Optional host-defined source identity, independent of texel contents.
+        std::function<uint32_t()> replacementContext;
+        std::function<uint64_t(uint32_t, const LoadOperation &, const LoadTile &, uint16_t, uint16_t, uint32_t, uint32_t)> sourceReplacement;
 
         void uploadEmpty(State *state, TextureCache *textureCache, uint64_t creationFrame, uint16_t width, uint16_t height, uint64_t replacementHash);
         uint64_t uploadTMEM(State *state, const LoadTile &loadTile, TextureCache *textureCache, uint64_t creationFrame, uint16_t byteOffset, uint16_t byteCount, uint16_t width, uint16_t height, uint32_t tlut);
@@ -22,4 +26,4 @@ namespace RT64 {
         void dumpTexture(uint64_t hash, State *state, const LoadTile &loadTile, uint16_t width, uint16_t height, uint32_t tlut);
         void removeHashes(const std::vector<uint64_t> &hashes);
     };
-};
\ No newline at end of file
+};
diff --git a/src/render/rt64_texture.h b/src/render/rt64_texture.h
index 95618bc..6e38193 100644
--- a/src/render/rt64_texture.h
+++ b/src/render/rt64_texture.h
@@ -10,6 +10,8 @@
 namespace RT64 {
     struct Texture {
         uint64_t creationFrame = 0;
+        uint64_t contentHash = 0;
+        uint64_t sourceHash = 0;
         std::unique_ptr<RenderTexture> texture;
         std::unique_ptr<RenderTexture> tmem;
         RenderFormat format = RenderFormat::UNKNOWN;
diff --git a/src/render/rt64_texture_cache.cpp b/src/render/rt64_texture_cache.cpp
index 950bc1b..efda95d 100644
--- a/src/render/rt64_texture_cache.cpp
+++ b/src/render/rt64_texture_cache.cpp
@@ -1149,6 +1149,8 @@ namespace RT64 {
                     newTexture->loadTile = upload.loadTile;
                     newTexture->tlut = upload.tlut;
                     newTexture->decodeTMEM = upload.decodeTMEM;
+                    newTexture->contentHash = upload.contentHash;
+                    newTexture->sourceHash = upload.sourceHash;
 
                     if (!upload.bytesTMEM.empty()) {
                         void *dstData = tmemUploadResources[i]->map();
@@ -1185,7 +1187,7 @@ namespace RT64 {
                         beforeDecodeBarriers.emplace_back(dstTexture->texture.get(), RenderTextureLayout::GENERAL);
                     }
 
-                    addResolvedPaths(upload.hash, upload.width, upload.height, upload.tlut, upload.loadTile, upload.bytesTMEM, upload.decodeTMEM, resolvedPathQueueCopy);
+                    addResolvedPaths(upload.hash, upload.width, upload.height, upload.tlut, upload.loadTile, upload.bytesTMEM, upload.decodeTMEM, resolvedPathQueueCopy, 0, upload.contentHash, upload.sourceHash);
                 }
 
                 replacementMapAdditions.clear();
@@ -1350,11 +1352,13 @@ namespace RT64 {
         }
     }
 
-    void TextureCache::queueGPUUploadTMEM(uint64_t hash, uint64_t creationFrame, const uint8_t *bytes, int bytesCount, int width, int height, uint32_t tlut, const LoadTile &loadTile, bool decodeTMEM) {
+    void TextureCache::queueGPUUploadTMEM(uint64_t hash, uint64_t creationFrame, const uint8_t *bytes, int bytesCount, int width, int height, uint32_t tlut, const LoadTile &loadTile, bool decodeTMEM, uint64_t contentHash, uint64_t sourceHash) {
         assert(!decodeTMEM || ((width > 0) && (height > 0)));
 
         TextureUpload newUpload;
         newUpload.hash = hash;
+        newUpload.contentHash = contentHash;
+        newUpload.sourceHash = sourceHash;
         newUpload.creationFrame = creationFrame;
         newUpload.width = width;
         newUpload.height = height;
@@ -1378,7 +1382,7 @@ namespace RT64 {
         });
     }
 
-    void TextureCache::addResolvedPaths(uint64_t hash, uint32_t width, uint32_t height, uint32_t tlut, const LoadTile &loadTile, const std::vector<uint8_t> &bytesTMEM, bool decodeTMEM, std::vector<ReplacementResolvedPath> &resolvedPaths, uint64_t exclusiveDbHash) {
+    void TextureCache::addResolvedPaths(uint64_t hash, uint32_t width, uint32_t height, uint32_t tlut, const LoadTile &loadTile, const std::vector<uint8_t> &bytesTMEM, bool decodeTMEM, std::vector<ReplacementResolvedPath> &resolvedPaths, uint64_t exclusiveDbHash, uint64_t contentHash, uint64_t sourceHash) {
         uint64_t hashes[TMEMHasher::CurrentHashVersion + 1] = {};
         for (uint32_t v : textureMap.replacementMap.resolvedHashVersions) {
             if (decodeTMEM && v < TMEMHasher::CurrentHashVersion) {
@@ -1386,7 +1390,7 @@ namespace RT64 {
                 hashes[v] = TMEMHasher::hash(bytesTMEM.data(), loadTile, width, height, tlut, v);
             }
             else {
-                hashes[v] = hash;
+                hashes[v] = contentHash ? contentHash : hash;
             }
         }
 
@@ -1394,10 +1398,12 @@ namespace RT64 {
             const auto &fsResolvedPaths = textureMap.replacementMap.fileSystemResolvedPaths[i];
             uint32_t hashVersion = textureMap.replacementMap.fileSystemHashVersions[i];
             if ((exclusiveDbHash == 0) || (exclusiveDbHash == hashes[hashVersion])) {
-                auto it = fsResolvedPaths.find(hashes[hashVersion]);
+                auto it = sourceHash ? fsResolvedPaths.find(sourceHash) : fsResolvedPaths.end();
+                if (it == fsResolvedPaths.end()) it = fsResolvedPaths.find(hashes[hashVersion]);
                 if (it != fsResolvedPaths.end()) {
                     // Stop searching for replacements as soon as we find one.
                     resolvedPaths.emplace_back(it->second);
+                    resolvedPaths.back().textureHash = hash;
                     break;
                 }
             }
@@ -1539,7 +1545,7 @@ namespace RT64 {
             for (size_t i = 0; i < textureMap.textures.size(); i++) {
                 if (textureMap.textures[i] != nullptr) {
                     Texture *texture = textureMap.textures[i];
-                    addResolvedPaths(textureMap.hashes[i], texture->width, texture->height, texture->tlut, texture->loadTile, texture->bytesTMEM, texture->decodeTMEM, resolvedPathQueue, exclusiveDbHash);
+                    addResolvedPaths(textureMap.hashes[i], texture->width, texture->height, texture->tlut, texture->loadTile, texture->bytesTMEM, texture->decodeTMEM, resolvedPathQueue, exclusiveDbHash, texture->contentHash, texture->sourceHash);
                 }
             }
         }
diff --git a/src/render/rt64_texture_cache.h b/src/render/rt64_texture_cache.h
index 9131697..2b90cb1 100644
--- a/src/render/rt64_texture_cache.h
+++ b/src/render/rt64_texture_cache.h
@@ -37,6 +37,8 @@ namespace interop {
 namespace RT64 {
     struct TextureUpload {
         uint64_t hash;
+        uint64_t contentHash = 0;
+        uint64_t sourceHash = 0;
         uint64_t creationFrame;
         uint32_t width;
         uint32_t height;
@@ -233,9 +235,9 @@ namespace RT64 {
         TextureCache(RenderWorker *directWorker, RenderWorker *copyWorker, uint32_t threadCount, const ShaderLibrary *shaderLibrary);
         ~TextureCache();
         void uploadThreadLoop();
-        void queueGPUUploadTMEM(uint64_t hash, uint64_t creationFrame, const uint8_t *bytes, int bytesCount, int width, int height, uint32_t tlut, const LoadTile &loadTile, bool decodeTMEM);
+        void queueGPUUploadTMEM(uint64_t hash, uint64_t creationFrame, const uint8_t *bytes, int bytesCount, int width, int height, uint32_t tlut, const LoadTile &loadTile, bool decodeTMEM, uint64_t contentHash = 0, uint64_t sourceHash = 0);
         void waitForGPUUploads();
-        void addResolvedPaths(uint64_t hash, uint32_t width, uint32_t height, uint32_t tlut, const LoadTile &loadTile, const std::vector<uint8_t> &bytesTMEM, bool decodeTMEM, std::vector<ReplacementResolvedPath> &resolvedPaths, uint64_t exclusiveDbHash = 0);
+        void addResolvedPaths(uint64_t hash, uint32_t width, uint32_t height, uint32_t tlut, const LoadTile &loadTile, const std::vector<uint8_t> &bytesTMEM, bool decodeTMEM, std::vector<ReplacementResolvedPath> &resolvedPaths, uint64_t exclusiveDbHash = 0, uint64_t contentHash = 0, uint64_t sourceHash = 0);
         bool useTexture(uint64_t hash, uint64_t submissionFrame, uint32_t &textureIndex, interop::float2 &textureScale, interop::float3 &textureDimensions, bool &textureReplaced, bool &hasMipmaps, bool &shiftedByHalf);
         bool useTexture(uint64_t hash, uint64_t submissionFrame, uint32_t &textureIndex);
         bool addReplacement(uint64_t hash, const std::string &relativePath, ReplacementShift shift);
@@ -265,4 +267,4 @@ namespace RT64 {
         static bool setLowMipCache(RenderDevice *device, RenderCommandList *commandList, const uint8_t *bytes, size_t byteCount, std::unique_ptr<RenderBuffer> &dstUploadResource, std::unordered_map<std::string, LowMipCacheTexture> &dstTextureMap, uint64_t &totalMemory);
         static Texture *loadTextureFromBytes(RenderDevice *device, RenderCommandList *commandList, const std::vector<uint8_t> &fileBytes, std::unique_ptr<RenderBuffer> &dstUploadResource, RenderPool *resourcePool = nullptr, std::mutex *uploadResourcePoolMutex = nullptr);
     };
-};
\ No newline at end of file
+};
